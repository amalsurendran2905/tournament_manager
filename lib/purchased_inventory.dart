import 'package:flutter/material.dart';

class PurchasedInventory extends StatelessWidget {
  const PurchasedInventory({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      
    );
  }
}